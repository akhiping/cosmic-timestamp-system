"\"\"\"\n+Serve the frontend HTML via Vercel serverless (Python runtime).\n+\"\"\"\n+import os\n+\n+\n+def handler(request):\n+    \"\"\"Return the single-page app HTML for any non-API route.\"\"\"\n+    base_dir = os.path.dirname(os.path.abspath(__file__))\n+    # templates sits one level up from /api\n+    template_path = os.path.join(base_dir, '..', 'templates', 'index.html')\n+\n+    try:\n+        with open(template_path, 'r', encoding='utf-8') as f:\n+            html_content = f.read()\n+\n+        return {\n+            'statusCode': 200,\n+            'headers': {\n+                'Content-Type': 'text/html; charset=utf-8',\n+                'Cache-Control': 'public, max-age=0, must-revalidate'\n+            },\n+            'body': html_content\n+        }\n+    except Exception as exc:  # pragma: no cover - simple fail-safe\n+        return {\n+            'statusCode': 500,\n+            'headers': {'Content-Type': 'text/plain'},\n+            'body': f'Error loading page: {exc}'\n+        }\n+

